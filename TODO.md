
* isotope-fine-structure-tool:
  * Step 3: resolution criterion -- currently uses R = m/Δm (FWHM = peak separation), which gives ~94% valley height (no separation). Fix to a practical criterion: e.g. R where two peaks show a discernible dip or centroiding finds separate peaks.
  * step 4: do an interactive plot so you can zoom